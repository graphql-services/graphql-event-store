type User {
  username: String!
  password: String
  age: Int
}

enum RoleType {
  ADMIN
  USER
}

type Role {
  name: String
  type: RoleType
}

type Company {
  name: String!
  employees: [User!]!
}
